const XLSX = require('xlsx');

function parse() {
    // try to find the file user uploaded? We don't have it on disk unless they put it in Desktop/Sales
    // Let's just create a test dummy insert for missing products
}
